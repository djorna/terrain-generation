cmake_minimum_required(VERSION 2.8)
add_compile_options(-std=c++11)
project( terrain_generation)
find_package( OpenCV REQUIRED )
link_directories(~/Documents/TerrainGeneration)
add_executable( terrain_generation terrain_generation.cpp )
target_link_libraries( terrain_generation ${OpenCV_LIBS} )